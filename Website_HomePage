<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VeloceBridge - A Better Way to Borrow and Lend</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #0a1a1a 0%, #1a2f2f 50%, #0f1f1f 100%);
            color: white;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .container {
            max-width: 1920px;
            margin: 0 auto;
            padding: 0 2rem;
            position: relative;
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.5rem 0;
            position: relative;
            z-index: 10;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 700;
            color: white;
        }

        .nav {
            display: flex;
            gap: 2.5rem;
            list-style: none;
        }

        .nav a {
            color: #a0a0a0;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .nav a:hover {
            color: #ffd700;
        }

        /* Main Content */
        .main-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            min-height: 80vh;
            padding: 2rem 0;
        }

        .hero-section {
            position: relative;
            z-index: 5;
        }

        .hero-title {
            font-size: 4rem;
            font-weight: 700;
            line-height: 1.1;
            margin-bottom: 3rem;
            background: linear-gradient(135deg, #ffffff 0%, #e0e0e0 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .cta-buttons {
            display: flex;
            gap: 2rem;
            margin-bottom: 3rem;
            justify-content: center;
        }

        .btn {
            padding: 1.2rem 2.8rem;
            border: none;
            border-radius: 16px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.8rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn-icon {
            font-size: 1.3rem;
            filter: drop-shadow(0 2px 4px rgba(0,0,0,0.3));
        }

        .btn-lend {
            background: linear-gradient(135deg, #2d8f7f 0%, #1a5f5f 100%);
            color: white;
            box-shadow: 0 6px 25px rgba(45, 143, 127, 0.4);
        }

        .btn-lend:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(45, 143, 127, 0.5);
        }

        .btn-borrow {
            background: linear-gradient(135deg, #ffd700 0%, #ffb700 100%);
            color: #1a1a1a;
            box-shadow: 0 6px 25px rgba(255, 215, 0, 0.4);
        }

        .btn-borrow:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(255, 215, 0, 0.5);
        }

        /* Loan Calculator */
        .loan-calculator {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 3rem;
            position: relative;
            overflow: hidden;
        }

        .loan-calculator::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(45, 143, 127, 0.1) 0%, transparent 70%);
            animation: pulse 4s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .calculator-content {
            position: relative;
            z-index: 2;
        }

        .calculator-title {
            font-size: 1.6rem;
            font-weight: 600;
            margin-bottom: 2.5rem;
            color: #ffffff;
            text-align: center;
        }

        .form-group {
            margin-bottom: 2.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 1rem;
            color: #b0b0b0;
            font-weight: 500;
            font-size: 1rem;
        }

        .slider-container {
            position: relative;
        }

        .slider {
            width: 100%;
            height: 8px;
            border-radius: 4px;
            background: linear-gradient(90deg, #2d8f7f 0%, #1a5f5f 100%);
            outline: none;
            -webkit-appearance: none;
            margin-bottom: 1rem;
        }

        .slider::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 28px;
            height: 28px;
            border-radius: 50%;
            background: #ffd700;
            cursor: pointer;
            box-shadow: 0 4px 12px rgba(255, 215, 0, 0.4);
            transition: transform 0.2s ease;
        }

        .slider::-webkit-slider-thumb:hover {
            transform: scale(1.1);
        }

        .slider::-moz-range-thumb {
            width: 28px;
            height: 28px;
            border-radius: 50%;
            background: #ffd700;
            cursor: pointer;
            border: none;
        }

        .slider-labels {
            display: flex;
            justify-content: space-between;
            font-size: 0.9rem;
            color: #888;
        }

        .calculation-display {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 16px;
            padding: 2rem;
            margin: 2rem 0;
            text-align: center;
        }

        .amount-display {
            font-size: 2.2rem;
            font-weight: 700;
            color: #ffd700;
            margin-bottom: 1rem;
        }

        .emi-display {
            font-size: 1.4rem;
            font-weight: 600;
            color: #2d8f7f;
            margin-bottom: 0.5rem;
        }

        .total-display {
            font-size: 1rem;
            color: #b0b0b0;
        }

        .apply-btn {
            width: 100%;
            margin-top: 1.5rem;
            justify-content: center;
        }

        /* Features Section */
        .features {
            margin-top: 6rem;
            text-align: center;
        }

        .features-title {
            font-size: 2.8rem;
            font-weight: 600;
            margin-bottom: 4rem;
            color: white;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 3rem;
            margin-top: 2rem;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 3rem 2rem;
            text-align: center;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(45, 143, 127, 0.1), transparent);
            transition: left 0.6s;
        }

        .feature-card:hover::before {
            left: 100%;
        }

        .feature-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
            border-color: rgba(45, 143, 127, 0.3);
        }

        .feature-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #ffd700 0%, #ffb700 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            font-size: 2rem;
            color: #1a1a1a;
            position: relative;
            z-index: 2;
        }

        .feature-title {
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: white;
            position: relative;
            z-index: 2;
        }

        .feature-desc {
            color: #b0b0b0;
            line-height: 1.6;
            font-size: 1rem;
            position: relative;
            z-index: 2;
        }

        /* Decorative Elements */
        .decoration {
            position: absolute;
            opacity: 0.05;
            pointer-events: none;
            color: #2d8f7f;
        }

        .decoration-1 {
            top: 15%;
            right: 8%;
            font-size: 12rem;
        }

        .decoration-2 {
            top: 55%;
            right: 3%;
            font-size: 8rem;
        }

        .decoration-3 {
            bottom: 15%;
            right: 12%;
            font-size: 6rem;
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .main-content {
                grid-template-columns: 1fr;
                gap: 3rem;
                text-align: center;
            }
            
            .hero-title {
                font-size: 3.5rem;
            }

            .cta-buttons {
                justify-content: center;
            }
        }

        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }
            
            .features-grid {
                grid-template-columns: 1fr;
                gap: 2rem;
            }
            
            .nav {
                display: none;
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 250px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Decorative Elements -->
        <div class="decoration decoration-1">₹</div>
        <div class="decoration decoration-2">📈</div>
        <div class="decoration decoration-3">🏛️</div>

        <!-- Header -->
        <header class="header">
            <div class="logo">VeloceBridge</div>
            <nav>
                <ul class="nav">
                    <li><a href="#invest">Invest</a></li>
                    <li><a href="#finance">Finance</a></li>
                    <li><a href="#about">About</a></li>
                    <li><a href="#rates">Rates</a></li>
                </ul>
            </nav>
        </header>

        <!-- Main Content -->
        <main class="main-content">
            <div class="hero-section">
                <h1 class="hero-title">A better way to borrow and lend</h1>
                <div class="cta-buttons">
                    <a href="#lend" class="btn btn-lend">
                        <span class="btn-icon">💰</span>
                        Lend
                    </a>
                    <a href="#borrow" class="btn btn-borrow">
                        <span class="btn-icon">🏦</span>
                        Borrow
                    </a>
                </div>
            </div>

            <div class="loan-calculator">
                <div class="calculator-content">
                    <h2 class="calculator-title">Personal Loan Calculator</h2>
                    
                    <div class="form-group">
                        <label for="amount">Loan Amount</label>
                        <input type="range" id="amount" class="slider" min="50000" max="5000000" value="500000" step="50000">
                        <div class="slider-labels">
                            <span>₹50K</span>
                            <span>₹50L</span>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="term">Loan Term (Months)</label>
                        <input type="range" id="term" class="slider" min="12" max="84" value="36" step="6">
                        <div class="slider-labels">
                            <span>1 Year</span>
                            <span>7 Years</span>
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="rate">Interest Rate (%)</label>
                        <input type="range" id="rate" class="slider" min="8" max="24" value="12" step="0.5">
                        <div class="slider-labels">
                            <span>8%</span>
                            <span>24%</span>
                        </div>
                    </div>

                    <div class="calculation-display">
                        <div class="amount-display" id="displayAmount">₹5,00,000</div>
                        <div class="emi-display" id="displayEMI">EMI: ₹16,607</div>
                        <div class="total-display" id="displayTotal">Total Amount: ₹5,97,852</div>
                    </div>
                    
                    <button class="btn btn-borrow apply-btn">
                        <span class="btn-icon">📋</span>
                        Apply Now
                    </button>
                </div>
            </div>
        </main>

        <!-- Features Section -->
        <section class="features">
            <h2 class="features-title">Why Choose VeloceBridge?</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">⚡</div>
                    <h3 class="feature-title">Instant Processing</h3>
                    <p class="feature-desc">AI-powered credit assessment delivers loan approvals within minutes, not days</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🛡️</div>
                    <h3 class="feature-title">Bank-Grade Security</h3>
                    <p class="feature-desc">256-bit encryption and RBI compliance ensure your data remains protected</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">📊</div>
                    <h3 class="feature-title">Market-Best Rates</h3>
                    <p class="feature-desc">Competitive interest rates starting from 8% with complete transparency</p>
                </div>
            </div>
        </section>
    </div>

    <script>
        // Loan Calculator with Real EMI Calculation
        const amountSlider = document.getElementById('amount');
        const termSlider = document.getElementById('term');
        const rateSlider = document.getElementById('rate');
        const displayAmount = document.getElementById('displayAmount');
        const displayEMI = document.getElementById('displayEMI');
        const displayTotal = document.getElementById('displayTotal');

        function formatCurrencyINR(amount) {
            // Indian number formatting with proper commas
            return new Intl.NumberFormat('en-IN', {
                style: 'currency',
                currency: 'INR',
                minimumFractionDigits: 0,
                maximumFractionDigits: 0
            }).format(amount);
        }

        function calculateEMI(principal, rate, tenure) {
            // Convert annual rate to monthly and percentage to decimal
            const monthlyRate = (rate / 100) / 12;
            
            // EMI calculation using the standard formula
            // EMI = P * r * (1+r)^n / ((1+r)^n - 1)
            const emi = (principal * monthlyRate * Math.pow(1 + monthlyRate, tenure)) / 
                       (Math.pow(1 + monthlyRate, tenure) - 1);
            
            return emi;
        }

        function updateCalculations() {
            const principal = parseInt(amountSlider.value);
            const tenure = parseInt(termSlider.value);
            const rate = parseFloat(rateSlider.value);
            
            const emi = calculateEMI(principal, rate, tenure);
            const totalAmount = emi * tenure;
            
            displayAmount.textContent = formatCurrencyINR(principal);
            displayEMI.textContent = `EMI: ${formatCurrencyINR(emi)}`;
            displayTotal.textContent = `Total Amount: ${formatCurrencyINR(totalAmount)}`;
        }

        // Add event listeners
        amountSlider.addEventListener('input', updateCalculations);
        termSlider.addEventListener('input', updateCalculations);
        rateSlider.addEventListener('input', updateCalculations);

        // Initialize calculations
        updateCalculations();

        // Smooth scroll for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Feature cards animation
        const cards = document.querySelectorAll('.feature-card');
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach((entry, index) => {
                if (entry.isIntersecting) {
                    setTimeout(() => {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }, index * 200);
                }
            });
        }, observerOptions);

        cards.forEach(card => {
            card.style.opacity = '0';
            card.style.transform = 'translateY(30px)';
            card.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
            observer.observe(card);
        });

        // Add some interactive feedback
        document.querySelectorAll('.btn').forEach(btn => {
            btn.addEventListener('click', function(e) {
                // Create ripple effect
                const ripple = document.createElement('div');
                const rect = this.getBoundingClientRect();
                const size = Math.max(rect.width, rect.height);
                
                ripple.style.width = ripple.style.height = size + 'px';
                ripple.style.position = 'absolute';
                ripple.style.left = (e.clientX - rect.left - size/2) + 'px';
                ripple.style.top = (e.clientY - rect.top - size/2) + 'px';
                ripple.style.background = 'rgba(255,255,255,0.3)';
                ripple.style.borderRadius = '50%';
                ripple.style.transform = 'scale(0)';
                ripple.style.animation = 'ripple 0.6s linear';
                ripple.style.pointerEvents = 'none';
                
                this.appendChild(ripple);
                
                setTimeout(() => {
                    ripple.remove();
                }, 600);
            });
        });

        // Add ripple animation
        const style = document.createElement('style');
        style.textContent = `
            @keyframes ripple {
                to {
                    transform: scale(2);
                    opacity: 0;
                }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>
